// The lightweight deployment uses the same dependency-free, print-ready HTML
// exporter. Recipients can open it in any browser and print or save it as PDF.
export { generateGuidePDF, generatePDFFilename } from "./pdfGenerator";
